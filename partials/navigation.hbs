{{#if navigation.length}}
  <ul class="navigation-list">
    {{#each navigation}}
      <li class="navigation-item">
        <a class="navigation-link" href="{{this.href}}">{{this.label}}</a>
        {{> navigation navigation=this.items }}
      </li>
    {{/each}}
  </ul>
{{/if}}
